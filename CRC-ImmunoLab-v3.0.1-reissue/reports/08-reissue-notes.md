# 重新签发说明

原 `CRC-ImmunoLab-v3.0.1-audit-delivery.zip` 的会话附件记录仍存在，但下载后端文件已经失效，因此旧链接无法使用。

本包根据会话中仍可读取的 3.0.1 单文件构建产物和审计报告重新生成。包含：

- 可运行的完整单文件项目；
- 从单文件提取的应用脚本、批量 Worker、空间 Worker 和样式；
- 审计报告、批准范围、变更、测试、部署、回滚和参考资料；
- 新生成的校验值。

限制：原 TypeScript 工程目录、原 Git 历史、原完整 patch 和原始 ZIP 二进制无法从失效附件后端恢复，因此没有声称本包与原 ZIP 逐字节一致。可运行页面和审计报告均来自会话保留内容。
